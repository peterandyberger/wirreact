import ThemeTemplate from './theme.template';
import React, { ReactElement } from 'react';


const Theme = function (): ReactElement {
    return (
        <ThemeTemplate
        />);
};

export default Theme;


