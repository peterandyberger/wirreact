import HeaderTemplate from './header.template';
import { ReactElement } from 'react';

const Header = function (): ReactElement {

    return <HeaderTemplate title={'Header'}/>;
};

export default Header;
